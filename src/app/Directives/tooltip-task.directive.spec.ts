import { TooltipTaskDirective } from './tooltip-task.directive';

describe('TooltipTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipTaskDirective();
    expect(directive).toBeTruthy();
  });
});
